exports.userUpdate =  (
    (req,res)=>{
        //get request from react app then backend sends response to it
        res.json({
            data:'Welcome to authentication router'
        })
        }
)